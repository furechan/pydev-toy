[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pydev-proto"
version = "0.0.0"
requires-python = ">=3.7"
dependencies = [ "click", "tomli", "build", "twine" ]

[tool.hatch.build]
sources = ["src"]
include = ["src/pydev"]

[project.scripts]
pydev = "pydev:main"

