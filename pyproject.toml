[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pydev-toy"
version = "0.0.2"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [ "click", "tomli", "build", "twine" ]

[dependency-groups]
dev = [ "pytest", "ipykernel", "invoke", "build", "twine" ]

[tool.hatch.build]
targets.wheel.sources = ["src"]
dev-mode-dirs = ["src"]
only-include = ["src"]
exclude = [".*"]

[project.scripts]
pydev = "pydev:main"

[tool.pydev]
allow-publish = true
